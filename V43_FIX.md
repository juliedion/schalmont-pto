V4.3

1. Removes the remaining grass artifacts at their real source:
   the far-left damaged pixels in the desktop Fort PNG are made transparent.
   The existing clean repeating grass layer now shows beneath them.

2. Restores the alien blink reliably:
   - explicit eyelid width AND height
   - exact desktop/mobile coordinates
   - visible teal eyelid
   - double-blink cycle every 4.8 seconds
   - overrides old reduced-motion rules that were hiding the blink
